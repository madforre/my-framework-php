내가 만든 PHP 프레임워크의 약점

- 우분투 방화벽이 설정되있지 않음.

- HTTPS 인증

- URL 필터 (유효성 검사) 가 안된다.

    어떤 면에서? .php 찍으면 내가 만든 404 페이지가 안 뜬다!
    autoload.php 라우터 사용자 설정 확인 전에 구현하다 말았다.
    동적으로 서버변수들을 부트스트래핑해서 원할 때 사용 하고 싶은데
    프로퍼티가 비공개라서 접근이 안되는 것 같은데 나중에 다시 보도록 하자.

- GET 방식 이외의 방식이 필터링이 안된다.

- 컨트롤러가 애매하다.

    컨트롤러의 역할에 대한 이해가 부족하다.

- $router = new Router(new Request);

    같은 방식을 통해서 동적으로 프로퍼티를 오버로딩한 Request 객체를 사용하고 싶은데 잘 안되는 중.